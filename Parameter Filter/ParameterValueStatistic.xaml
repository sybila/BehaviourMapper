<UserControl x:Class="Parameter_Filter.ParameterValueStatistic"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="80">
    <StackPanel>
        <TextBlock Text="{Binding Mask, Mode=OneWay}" Margin="5,5,5,0" HorizontalAlignment="Center" />
        <ItemsControl ItemsSource="{Binding Values, Mode=OneWay}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Margin="5,0,5,0" HorizontalAlignment="Center">
                        <Run Text="{Binding Context, Mode=OneWay}" />
                        <Run Text=" .. " />
                        <Run Text="{Binding Percentage, Mode=OneWay, StringFormat=0.00}" />
                        <Run Text="%" />
                    </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
