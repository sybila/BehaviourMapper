<UserControl x:Class="Parameter_Filter.ParameterInformation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="120" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.RowSpan="3" Text="{Binding Mask, Mode=OneWay}" TextWrapping="Wrap" />
        <TextBlock Grid.Column="1" Margin="10,5,10,0"><Run Text="Witnesses: " /><Run Text="{Binding WitnessCount, Mode=OneWay}" /></TextBlock>
        <TextBlock Grid.Column="1" Grid.Row="1" Margin="10,5,10,0"><Run Text="Shortest: " /><Run Text="{Binding ShortestWitness, Mode=OneWay}" /></TextBlock>
        <TextBlock Grid.Column="1" Grid.Row="2" Margin="10,5,10,5"><Run Text="Robustness: " /><Run Text="{Binding Robustness, Mode=OneWay, StringFormat=0.00}" /><Run Text="%"/></TextBlock>
    </Grid>
</UserControl>
