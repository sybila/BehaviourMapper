<UserControl x:Class="Parameter_Filter.WitnessInformation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="120" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.RowSpan="3" Text="{Binding Mask, Mode=OneTime}" TextWrapping="Wrap" />
        <TextBlock Grid.Column="1" Margin="10,5,10,0"><Run Text="Parameters: " /><Run Text="{Binding ParameterCount, Mode=OneWay}" /></TextBlock>
        <TextBlock Grid.Column="1" Grid.Row="1" Margin="10,5,10,0"><Run Text="Length: " /><Run Text="{Binding Length, Mode=OneTime}" /></TextBlock>
    </Grid>
</UserControl>
